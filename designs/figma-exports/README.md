# Figma Exports

Place Figma file links and exported assets here when the design source is synced.

Suggested naming: `home-page-{variant}.png` / `.pdf`.

For now, the mid-fidelity mockup is a browser-renderable HTML file at `../mockups/home-page.html`.
